<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Year | Arisha</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Poppins:wght@300;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --akashi: #87CEEB;
            --babypink: #FFB6C1;
            --gold: #FFD700;
        }

        body, html {
            margin: 0; padding: 0; width: 100%; height: 100%;
            overflow: hidden; font-family: 'Poppins', sans-serif;
            background: linear-gradient(-45deg, #e0f7fa, #fce4ec, #b2ebf2, #f8bbd0);
            background-size: 400% 400%;
            animation: gradientMove 15s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .chandelier-container {
            position: absolute; top: 0; width: 100%;
            display: flex; justify-content: space-around; z-index: 10;
        }

        .jharbati {
            width: 2px; height: 100px; background: var(--gold);
            position: relative; transition: all 1.5s ease-in;
        }

        .jharbati::after {
            content: '🕯️'; position: absolute; bottom: -15px; left: -10px;
            filter: drop-shadow(0 0 10px var(--gold)); font-size: 20px;
        }

        .heart {
            position: absolute; top: -20px;
            pointer-events: none; animation: heartFall linear forwards;
        }

        @keyframes heartFall {
            to { transform: translateY(105vh) rotate(360deg); }
        }

        .content {
            position: relative; z-index: 100; text-align: center;
            margin-top: 25vh;
        }

        h1 { font-size: 2rem; color: #555; font-weight: 300; margin:0; }
        .name { 
            font-family: 'Dancing Script', cursive; font-size: 5.5rem; 
            color: #ec407a; display: block; text-shadow: 2px 2px 10px rgba(0,0,0,0.05);
        }

        .magic-btn {
            background: white; color: #ec407a; border: 2px solid #ec407a;
            padding: 12px 40px; font-size: 1.1rem; border-radius: 30px;
            cursor: pointer; transition: 0.4s; font-weight: 600; margin-top: 30px;
            box-shadow: 0 4px 15px rgba(236, 64, 122, 0.2);
        }

        .magic-btn:hover { background: #ec407a; color: white; transform: scale(1.05); }

        #final-wish {
            display: none; margin-top: 20px; animation: fadeIn 2s;
        }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>

    <div class="chandelier-container" id="chandeliers">
        <div class="jharbati" style="height: 120px;"></div>
        <div class="jharbati" style="height: 160px;"></div>
        <div class="jharbati" style="height: 100px;"></div>
        <div class="jharbati" style="height: 140px;"></div>
    </div>

    <div class="content">
        <h1>Hello,</h1>
        <span class="name">Arisha</span>
        <button class="magic-btn" onclick="revealMagic()">Click for Surprise</button>
        
        <div id="final-wish">
            <h2 style="color: #007c91;">✨ Happy New Year 2026 ✨</h2>
            <p style="color: #666; font-size: 1.2rem;">Wishing you a year full of light and laughter.</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.5.1/dist/confetti.browser.min.js"></script>
    <script>
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = Math.random() > 0.5 ? '❤️' : '🩵';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.fontSize = (Math.random() * 12 + 10) + 'px';
            heart.style.opacity = Math.random() * 0.7 + 0.3;
            heart.style.animationDuration = (Math.random() * 3 + 4) + 's';
            document.body.appendChild(heart);
            setTimeout(() => heart.remove(), 7000);
        }
        setInterval(createHeart, 250);

        function revealMagic() {
            const jharbatis = document.querySelectorAll('.jharbati');
            jharbatis.forEach(j => {
                j.style.top = '100vh';
                j.style.opacity = '0';
            });

            document.getElementById('final-wish').style.display = 'block';
            document.querySelector('.magic-btn').style.display = 'none';

            var count = 200;
            var defaults = { origin: { y: 0.7 } };

            function fire(particleRatio, opts) {
                confetti(Object.assign({}, defaults, opts, {
                    particleCount: Math.floor(count * particleRatio)
                }));
            }

            fire(0.25, { spread: 26, startVelocity: 55, colors: ['#87CEEB', '#FFB6C1'] });
            fire(0.2, { spread: 60, colors: ['#FFD700'] });
            fire(0.35, { spread: 100, decay: 0.91, scalar: 1.2 });
        }
    </script>
</body>
</html>
